# Agent Tools

The project bootstraps the following portable tools under `build_cache/`:

- Go toolchain
- Zig toolchain
- msvcup and standalone MSVC/Windows SDK
- rustup and cargo
- Bun
- tree-sitter-cli installed with `--no-default-features`

These tools are used only for development and build orchestration.
